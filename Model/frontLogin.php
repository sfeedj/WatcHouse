<?php
function checkID(){
  echo 'TEST';
  // $req = $bdd->prepare('SELECT ID, password FROM users WHERE username = :pseudo'); $username, $password, $bdd
  // $req->execute(array(
  //     'pseudo' => $pseudo));
  // $resultat = $req->fetch();
  //
  // // Comparaison du pass envoyé via le formulaire avec la base
  // $isPasswordCorrect = password_verify($password, $resultat['password']);
  //
  // if (!$resultat)
  // {
  //   return false;
  // }
  // else
  // {
  //   if ($isPasswordCorrect) {
  //     $_SESSION['ID'] = $resultat['ID'];
  //     $_SESSION['username'] = $username;
  //     return true;
  //   }
  //   return false;
  // }
}
?>
